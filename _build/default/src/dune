(library
  (name turn)
  (modules turn)
  (libraries 
    core
    base)
  (preprocess (pps ppx_jane bisect_ppx)))

(library
  (name facelet)
  (modules facelet)
  (libraries 
    core
    base)
  (preprocess (pps ppx_jane bisect_ppx)))

(library
  (name cube)
  (modules cube)
  (libraries 
    core
    base
    turn
    facelet)
  (preprocess (pps ppx_jane bisect_ppx)))